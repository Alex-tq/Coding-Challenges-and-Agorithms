function whatIsInAName(collection, source) {
  // What's in a name?
  var arr = [];
  // Only change code below this line
  console.log(source.Keys()+'------');
  arr = source.keys();

  // Only change code above this line
  return arr;
}
let a = 5;
let pr = whatIsInAName([{ first: "Romeo", last: "Montague" }, { first: "Mercutio", last: null }, { first: "Tybalt", last: "Capulet" }], { last: "Capulet" });
console.log('533333333333333333333');